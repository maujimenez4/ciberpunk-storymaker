{
  "name": "ciberpunk-storymaker",
  "version": "0.1.0",
  "description": "Sistema agéntico que escribe novelas largas capítulo a capítulo, con estado explícito en disco y puntos de control humanos.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node tools/run.js",
    "seco": "node tools/run.js --profile smoke-3ch --dry-run",
    "invariantes": "node tools/run.js --check-only"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.273"
  }
}
