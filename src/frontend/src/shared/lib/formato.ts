/** Utilidades de presentación. Sin lógica de negocio y sin saber de features. */

/** «Capítulo 3», para índice y navegación. Un sitio y solo uno. */
export function nombreDeCapitulo(numero: number): string {
  return `Capítulo ${numero}`;
}
